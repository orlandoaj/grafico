export default class Const {

    constructor() {
        super();

        // Window size
        const WIDTH = 1920;
        const HEIGHT = 1080;

        // Center of window
        const CENTERX = WIDTH / 2;
        const CENTERY = HEIGHT / 2;
    }

}